export default (id: string) => `
<!DOCTYPE html>
    <script>
      // @ts-ignore
      const id = "${id}";
      const STORAGE_KEYS = {
        openFiles: \`openFiles-\${tabId}\`,
      };
    </script>
</html>
`;
